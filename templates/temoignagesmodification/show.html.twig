{% extends 'base.html.twig' %}

{% block title %}Temoignage{% endblock %}

{% block body %}
    <h1>Temoignage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temoignage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ temoignage.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_temoignagesmodification_index') }}">back to list</a>

    <a href="{{ path('app_temoignagesmodification_edit', {'id': temoignage.id}) }}">edit</a>

    {{ include('temoignagesmodification/_delete_form.html.twig') }}
{% endblock %}
