{{ form_start(carForm) }}
    <div class="form-group">
        {{ form_label(carForm.name, 'Nom', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(carForm.name, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(carForm.description, 'Description', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(carForm.description, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(carForm.price, 'Prix', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(carForm.price, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(carForm.distance, 'Distance', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(carForm.distance, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(carForm.year, 'Année', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(carForm.year, {'attr': {'class': 'form-control'}}) }}
    </div>
    <button type="submit" class="btn btn-primary">Valider</button>
{{ form_end(carForm) }}
