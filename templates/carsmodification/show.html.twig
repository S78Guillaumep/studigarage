{% extends 'base.html.twig' %}

{% block title %}Cars{% endblock %}

{% block body %}
    <h1>Cars</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ car.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_carsmodification_index') }}">back to list</a>

    <a href="{{ path('app_carsmodification_edit', {'id': car.id}) }}">edit</a>

    {{ include('carsmodification/_delete_form.html.twig') }}
{% endblock %}
