{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ service.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cartemodification_index') }}">back to list</a>

    <a href="{{ path('app_cartemodification_edit', {'id': carte.id}) }}">edit</a>

    {{ include('cartemodification/_delete_form.html.twig') }}
{% endblock %}
